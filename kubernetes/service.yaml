apiVersion: v1
kind: Service
metadata:
  name: diagnostic-bot
  labels:
    app.kubernetes.io/name: diagnostic-bot
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: diagnostic-bot
  ports:
    - name: metrics
      port: 9090
      targetPort: metrics
      protocol: TCP
